@page "/ParentCounter"
<CounterWithParameterAndEvent IncrementAmount="@incrementamount" @bind-CurrentCount="currentcount"></CounterWithParameterAndEvent>

The current count is: @currentcount

@code {
    int incrementamount = 10;
    int currentcount = 0;

}
